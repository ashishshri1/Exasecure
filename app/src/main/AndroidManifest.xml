<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ashishshrivastav.exasecure">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Signup" />
        <activity android:name=".Login" />
        <activity android:name=".Portal" />
        <activity android:name=".Add_Account" />
        <activity android:name=".Know_account" />
        <activity android:name=".Details" />
        <activity android:name=".MyAccounts" />
        <activity android:name=".Delete_Account" />
        <activity android:name=".Change_Key" />
        <activity android:name=".About"></activity>
    </application>

</manifest>